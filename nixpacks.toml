[phases.setup]
aptPkgs = ["python3", "python3-pip"]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip3 install -r requirements.txt || python3 -m pip install -r requirements.txt"]

[start]
cmd = "python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
